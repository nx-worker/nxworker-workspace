{
  "$schema": "https://json-schema.org/schema",
  "$id": "MoveFile",
  "title": "Move File Generator",
  "description": "Move a file from one Nx project to another and update import paths",
  "type": "object",
  "properties": {
    "source": {
      "type": "string",
      "description": "Path to the source file relative to workspace root",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the source file path (relative to workspace root)?"
    },
    "target": {
      "type": "string",
      "description": "Path to the target file relative to workspace root",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "x-prompt": "What is the target file path (relative to workspace root)?"
    }
  },
  "required": ["source", "target"]
}
