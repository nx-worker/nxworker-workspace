diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index original..fixed 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -103,11 +103,16 @@ jobs:
       # Nx Affected runs only tasks affected by the changes in this PR/commit. Learn more: https://nx.dev/ci/features/affected
       # Exclude benchmark tests (*.bench.spec.ts and performance-benchmark.spec.ts) from regular test runs
       # Run with coverage on ubuntu-24.04-arm with Node 22 to enforce coverage thresholds
       - name: Run tests
         shell: bash
         run: |
           COVERAGE_FLAG=""
           if [[ "${{ matrix.os }}" == "ubuntu-24.04-arm" && "${{ matrix.node-version }}" == "22" ]]; then
             COVERAGE_FLAG="--coverage"
           fi
-          npx nx affected -t test --configuration=ci $COVERAGE_FLAG --output-style=static --parallel=1 -- --testPathIgnorePatterns='benchmarks|performance-benchmark'
+
+          # Use retry wrapper to handle Nx 19.8.14 terminalOutputs cache bug
+          # The wrapper detects ENOENT errors for .nx/cache/terminalOutputs and retries
+          # If tests pass but only cache write fails, treats as success
+          bash tools/scripts/run-tests-with-retry.sh \
+            npx nx affected -t test --configuration=ci $COVERAGE_FLAG --output-style=static --parallel=1 -- --testPathIgnorePatterns='benchmarks|performance-benchmark'
